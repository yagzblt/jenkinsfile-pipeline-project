pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'I\'m about to unalive myself'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}
